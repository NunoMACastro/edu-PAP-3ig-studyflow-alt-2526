# Fixture válida

```ts
class SafeService {
    constructor(private readonly governedAi: GovernedAiExecutionService) {}
}

const session = JSON.stringify({ userId, sessionVersion });
await runner.enqueue(job);
```

O Playwright E2E não é opcional no gate final.

## Tutorial público

- `estado`: `TODO|DONE`
- Backend copiável: `apps/api/src/modules/example/example.service.ts`.
- Frontend copiável: `apps/web/src/features/example/example-panel.tsx`.

| bk_id | estado |
| --- | --- |
| BK-MF0-01 | DONE |

## Evidence da implementação de referência — fonte privada

### Ficheiros verificados

- `real_dev/api/src/modules/example/example.service.ts`
- `real_dev/web/src/features/example/example-panel.tsx`
